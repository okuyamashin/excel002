# `data` ディレクトリ配下の TSV 出力（仕様ドラフト）

実装はこのドキュメントの後続タスクとする。ここでは **`{tmp.dir}/{md5}/data/`**（概念として `/tmp/[md5]/data/`）に置く TSV 群の取り決めを書く。既存の `sheets.json` と同じディレクトリに並ぶ想定。

---

## 配置・ファイル名

- **ディレクトリ**: `{tmp.dir}/{md5}/data/`  
  - `tmp.dir` は `excel002.properties` の `tmp.dir`（未設定時はプロジェクトの慣例どおり `tmp` など）。
- **ファイル名**: **`{sheet_id}.{aspect}.tsv`**
  - **`sheet_id`**: ブック内のシート ID（`xl/workbook.xml` の `<sheet sheetId="…">`。既に `sheets.json` の各シートの `sheet_id` と同一）。
  - **`aspect`**: セルごとの観点（後述）。例: `value`, `string`, `format`, `formatted_value`, `type`, `formula`, `merge`, `style`。
- **拡張**: 必要になったら `aspect` を増やす（例: `comment`, `hyperlink` など）。このドキュメントに追記する。

例:

- `1.value.tsv`
- `1.format.tsv`
- `2.formatted_value.tsv`

ブック単位（シートに依存しない）のファイル:

- **`strings.ndjson`** … 共有文字列テーブル（NDJSON）。各 `{sheet_id}.string.tsv` は **`strings.ndjson`** の **`id`**（`sharedStrings.xml` の **`si` の出現順＝0 始まりインデックス**）を参照する。
- **`styles.ndjson`** … スタイル定義の辞書（NDJSON）。各 `{sheet_id}.style.tsv` はここで定義された **`id` を参照**する。

## グリッド形状（行・列）

- 対象シートについて、**論理グリッドはすべての aspect で共通**とする。
- **行数・列数**は、そのシートの使用範囲に合わせる（`sheets.json` の同一シートの `max_row_num` / `max_column_num` と整合させる想定。いずれも **1 始まりの最大行・列**でカウントしている前提）。
- **TSV の形**: **1 行がワークシートの 1 行**、**タブ（`\t`）区切りで列**。セル数は常に「そのシートの列数」に揃える。
- **空セル**: 該当セルにデータがない場合も **列は欠かさず**、フィールドは空文字（連続タブ、行末タブなしの行末など、通常の TSV の空フィールド）とする。

### ブック更新のフェーズ（行・列の変更とセル内容の変更）

構造変更とセル内容の変更を **順序を切って**扱う。

1. **フェーズ A（先行）**: **行の追加・削除、列の追加・削除**のみを OOXML / 内部モデルに適用する。これによりシートの **使用範囲（行数・列数）** が確定する。
2. **フェーズ B（中間）**: 確定したグリッドに合わせて **`data/` 配下の TSV と `sheets.json`（`max_row_num` / `max_column_num` 等）を作り直す**。以降の編集の **基準** とする。
3. **フェーズ C（後続）**: **値・表示・型・式・マージ・スタイル** など、セル内容の更新を行う（差分キューでも局所パッチでも可。実装で選ぶ）。

**原則**: 行・列の次元が変わる操作の **あとで** TSV を再生成し、**その TSV に対して** 値・スタイルの更新を積む。同じトランザクション内で「行挿入」と「古い行番号前提のセル更新」を混在させない。

**実装上の注意**: フェーズ A のあと、**式（`formula`）の A1 参照**や **マージ（`merge`）** はグリッドに整合した内容になるよう、ブック側で正規化するか、フェーズ B のエクスポート結果に反映されていること。

---

## エンコーディング

- **ファイル**: UTF-8（BOM なしを既定とする。実装時に BOM 付与する場合はこの文書を更新）。
- **改行**: LF（`\n`）を推奨。実装がプラットフォーム依存になる場合は実装側で明記する。

---

## Base64 を必須とする aspect

次の **aspect のセル内容は Base64** とする（デコード後は **UTF-8 の文字列**として解釈する前提）。

| aspect             | 備考 |
|--------------------|------|
| `value`            | セルの「格納値」側の文字列表現（型・由来は `type` で補足）。 |
| `formatted_value`  | ユーザーがシート上で見ている表示文字列に相当するもの（実装時に Excel と完全一致できない場合は仕様で許容範囲を書く）。 |

- **エンコード対象**: UTF-8 バイト列を Base64（標準の Base64 アルファベット、パディング `=` あり。URL-safe は使わない既定）。
- **空セル**: ファイル上は **空フィールド**とし、Base64 文字列は書かない（「空文字をエンコードした結果」を書かない）。

それ以外の aspect（`string`, `format`, `type`, `formula`, `merge`, `style` など）については、**別途この表に追記するまでプレーンテキスト**を既定とする（必要なら将来「この aspect も Base64」と追記する）。

---

## 各 aspect の意味（現時点）

実装時に OOXML（`sheet*.xml`, `sharedStrings.xml`, `styles.xml` 等）からどう埋めるかは別設計とし、ここでは **ファイルが表す意味**だけ置く。

| aspect             | セルあたりの内容のイメージ |
|--------------------|----------------------------|
| `value`            | 内部値・素の値のテキスト（Base64）。共有文字列インデックスではなく解決後の文字列、数値はその文字列表現など、実装で定義。 |
| `string`           | **共有文字列 ID**（プレーンテキスト）：`<c t="s">` の `<v>` に対応する **`strings.ndjson` の `id`**（10 進文字列）。共有文字列でないセルは空フィールド。 |
| `formatted_value` | 表示用文字列（Base64）。 |
| `format`           | 数値書式コードやスタイル由来の書式キー等（プレーンテキスト想定。詳細は実装時に固定）。 |
| `type`             | セルの種別（例: `number`, `string`, `boolean`, `error`, `blank` など。列挙は実装時に確定）。 |
| `formula`          | 数式（`sheet*.xml` の `<f>` 相当、あるいは空）。プレーンテキスト。 |
| `merge`            | 結合セル情報（プレーンテキスト）。詳細は下記「`merge` aspect」の規約。 |
| `style`            | **スタイル ID**（プレーンテキスト）。セルの **`cellXf`** に対応するインデックス等と **`styles.ndjson`** の各行の `id` を対応させる（キー名は実装で確定し、この文書に追記）。 |

### `merge` aspect

- ほとんどのセルには **`0,0`** のような値が入る（詳細なコード値は実装で確定）。
- 結合範囲で「代表となるセル」などには **`0,1`** のような別コードが入る想定。
- **結合に飲み込まれたセル（本体ではない側）** には **結合元（左上角）セルの座標を、`(col,row)` 形式で入れる**。`col` / `row` はいずれもワークシート上の **1 始まり**（列番号・行番号。A1 なら `col=1`, `row=1`）。
- **`0,0`、`0,1`、`(col,row)` 以外**：再構築実装では、旧データ向けに **`-1,-1`** も読み替えられる（エクスポートでは出力しない）。

### 共有文字列辞書 **`strings.ndjson`**（NDJSON）

- **配置**: `{tmp.dir}/{md5}/data/strings.ndjson`
- **形式**: **1 行に JSON オブジェクトがちょうど 1 個**。`sharedStrings.xml` の **`si` のドキュメント順**が **`id` の `"0"`, `"1"`, …** に対応する。
- **実装（現状）のキー**: **`schema_version`**、**`id`**、**`value`**（本文）。`sharedStrings.xml` が無いブックは **空ファイル**。

### スタイル辞書 **`styles.ndjson`**（NDJSON）

セル単位の `{sheet_id}.style.tsv` には **参照 ID のみ** を並べ、スタイル定義の本体は **`xl/styles.xml` をまとめた別ファイル**に載せる。フォーマットは **NDJSON（JSON Lines）** とする。

- **配置**: `{tmp.dir}/{md5}/data/styles.ndjson`（ファイル名は実装時まで **`styles.ndjson`** でよい。変更する場合はこの文書を更新）。
- **形式**: **1 行に JSON オブジェクトがちょうど 1 個**。ファイル全体を **配列 `[...]` で囲まない**。
- **エンコーディング**: UTF-8、改行は **`LF（`\n`）`** でレコードを区切る。
- **各行オブジェクト**には最低でも **`id`**（または `cell_xf` など、実装で固定したキー）を含み、`{sheet_id}.style.tsv` のセルに書いた **スタイル ID 文字列と同一の値**で参照できること。
- **実装（現状）のキー**: **`schema_version`**（数値 `1`）、**`id`**（`cellXfs` インデックスの文字列）、**`numFmtId`**、**`formatCode`**（組み込み・カスタム解決後）、**`restore_xml`**（単体でパース可能な `<xf xmlns="…">…</xf>` 断片）。
- **その他のキー**: 将来 `fonts` / `fills` / `borders` などを載せる余地あり。完全な JSON スキーマは実装とともにこの文書へ追記する。
- **行の並び**:既定では **`cellXfs` のインデックス昇順**（`id` がインデックスの文字列表現など）とするのがわかりやすい。別順にする場合は実装とともに明記する。

---

## `sheets.json` との関係

- `sheets.json` はシート一覧・`sheet_id`・ワークシート XML の相対パスなどのメタデータを提供する。
- **`strings.ndjson`** は **`sheets.json` と同じ `data/`** に置く共有文字列辞書であり、`string.tsv` の **`id` と同一の値**で参照できること。
- **`styles.ndjson`** は **`sheets.json` と同じ `data/` に置くブック単位のファイル**であり、全シート共通のスタイル定義を NDJSON で載せる。
- TSV は **`sheet_id` 単位**で同一 `data` ディレクトリに並ぶため、JSON の `sheet_id` とファイル名の `{sheet_id}` が対応する。
- ワークシート XML のパスは JSON の `worksheet_xml`（excel ルート相対）で引ける。

---

## タブ・改行を含む値の扱い（プレーンテキスト aspect）

Base64 **ではない**フィールドにタブや改行が含まれる場合、実装時に次のいずれかを選び、この文書を更新する。

- **推奨案**: CSV と同様の引用ルール（フィールドを `"` で囲み、`"` を `""` でエスケープ）を TSV に適用するかどうか。
- **代替案**: 禁止文字をエスケープシーケンスに置換する。

現時点では **未確定**とし、実装前に一方に決める。

---

## 今後のメモ

- 実装するタイミングで **`Book` のどのメソッドから出力するか**（例: `sheetsjson()` の直後、別メソッド）を決める。
- パフォーマンス（大きいブック）ではストリーミング書き出しを検討する。
- `sheet_id` にファイルシステムで無効な文字が来た場合のファイル名エスケープ規則が必要か検証する（通常の Excel は数値文字列が多い）。
